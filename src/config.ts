import type { BotConfig } from "../types/global";

const config: BotConfig = {
  staff: {
    staffRoleIds: ["", ""],
  },
  tickets: {
    parentCategoryId: "",
    closedCategoryId: "",
  },
};

export default config;
